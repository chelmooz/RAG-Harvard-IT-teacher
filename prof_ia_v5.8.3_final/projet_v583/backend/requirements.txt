# =============================================================================
# Prof IA v5.8 ALL-IN-ONE — Requirements (AMD BC-250 / ROCm 7.2 / Python 3.13)
# =============================================================================
# MIGRATION v5.8 : ajout chromadb + pyarrow (import datasets Arrow HuggingFace)
#
# PyTorch ROCm doit être installé SÉPARÉMENT (avant les autres) :
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/rocm7.2
# =============================================================================

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# ── Base de données ────────────────────────────────────────────────────────────
asyncpg==0.29.0
pgvector==0.3.3           # conservé pour les conversations PostgreSQL

# ── ChromaDB — vecteurs RAG ALL-IN-ONE (v5.8) ─────────────────────────────────
chromadb==0.4.22

# ── Apache Arrow — format HuggingFace natif (import_datasets.py) ───────────────
pyarrow>=14.0.1

# ── LangChain (chunking uniquement) ───────────────────────────────────────────
langchain-text-splitters==0.2.4

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.0.1
transformers==4.43.0
accelerate==0.33.0

# ── Traitement documents ───────────────────────────────────────────────────────
pypdf==5.1.0
python-docx==1.1.0
python-pptx==1.0.0
openpyxl==3.1.5

# ── Audio / Transcription ─────────────────────────────────────────────────────
openai-whisper==20240930
ffmpeg-python==0.2.0

# ── API / Async ───────────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==24.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Config & Validation ───────────────────────────────────────────────────────
pydantic==2.8.0
pydantic-settings==2.4.0
python-dotenv==1.0.1

# ── Logging ────────────────────────────────────────────────────────────────────
loguru==0.7.2

# ── ML / Évaluation ───────────────────────────────────────────────────────────
scikit-learn==1.5.0
numpy>=1.26,<3
pandas==2.2.2

# ── Fine-tuning ────────────────────────────────────────────────────────────────
peft==0.12.0
datasets==2.21.0
trl==0.10.0
pyyaml==6.0.2
huggingface-hub==0.24.5

# ── Docker SDK (contrôle services depuis dashboard)
docker==7.0.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.23.8
